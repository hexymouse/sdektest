spring:
  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:3306/entity?serverTimezone=UTC
    username: mysql
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver

  liquibase:
    change-log: classpath:liquibase/db-changelog.yaml